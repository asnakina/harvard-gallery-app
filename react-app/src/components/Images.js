import React from 'react';

function Images(props) {
  return (
    <div>
    <img src={props.imageData} className="imgClass"/>
  </div>
 )
}

export default Images;
  // <button type="button" onClick = {prop.ha}>Next</button>
